# Бриф: данные для Telegram-бота продаж «Онлайн Гимназия №1» — черновик заполнения

> Источник: оригинальный бриф — [Google Docs, «Бриф: данные для Telegram-бота продаж «Онлайн Гимназия №1»»](https://docs.google.com/document/d/1WYSAWSGN2h5JCGPhBF8_0j3lLKwosczx9z5_dC0FpfI/edit).
> В оригинале заполнены только разделы 1.1 (Шаблон А) и 1.5 (слова-сигналы). Ниже — попытка
> дозаполнить остальное на основе того, что уже зафиксировано в этом репозитории (`og1/PLAN.md`,
> код `catalog.py`/`kp.py`/`prompts.py`) и данных с сайта [og1.ru](https://og1.ru). Всё, что
> помечено **⚠️ ТРЕБУЕТСЯ ОТ ЗАКАЗЧИКА**, — реальных данных нет ни в брифе, ни на сайте, я не
> подставлял выдумку. Проверь и поправь прямо в этом файле или верни правки в исходный Google Doc.

---

## 1. Контент для бота (голос бизнеса, воронка, тарифы)

### 1.1. Шаблон А — голос бизнеса

*(без изменений — уже заполнено заказчиком в оригинале, дублирую для целостности)*

| Вопрос | Ответ |
|---|---|
| Тон общения | экспертный, без канцелярита и без фамильярности |
| Обращение к родителю | на «вы» |
| Фразы/слова, которые нельзя использовать | Не давать абсолютных обещаний: «гарантируем поступление», «гарантируем 100 баллов», «ваш ребёнок точно сдаст ЕГЭ», «лучшее образование», «идеальная школа» без доказуемого основания. Не называть обучение просто «домашним», если речь о зачислении и полноценном школьном образовании. |
| Фразы/формулировки, которые обязательны (УТП, гарантии) | Лицензия и государственная аккредитация; зачисление в школу; аттестат государственного образца; живые онлайн-уроки; малые классы; ручная проверка домашних заданий; поддержка куратора; подготовка к ОГЭ/ЕГЭ; записи уроков; социализация. Сильный CTA: «Попробуйте бесплатно» / «Запишитесь на бесплатный пробный день». |
| Примеры «удачных» ответов менеджера | «Куратор следит за посещаемостью и успеваемостью и поддерживает связь с родителями». «Если ребёнок пропустил занятие, он сможет посмотреть его в записи». |

**Закрыто 2026-08-12.** `TEMPLATE_A_OG1` в `prompts.py` переписан дословно под эту таблицу:
явный список запретов («гарантируем поступление» и т.п.), обязательные УТП (лицензия,
аккредитация, куратор, малые классы, записи уроков...) и оба примера удачного тона из брифа.

### 1.2. Воронка этапов

Дозаполнено по логике сайта (og1.ru: пробный день → консультация → тариф → документы → договор →
оплата → доступ) и уже существующей `FUNNEL_STAGES_OG1` в `prompts.py`. Тексты — черновые,
выдержаны в тоне Шаблона А (1.1), **не финальная формулировка**.

| № | Этап | Что происходит / что говорит бот |
|---|---|---|
| 1 | Интерес | Лид написал в личку или замечен по слову-сигналу в родительской группе (1.5). Бот (userbot) отвечает по Шаблону А: без давления, кратко по сути вопроса, с упоминанием лицензии/аккредитации где уместно. При тёплом/горячем балле — CTA «Попробуйте бесплатно» / приглашение на пробный день. |
| 2 | Пробный день | Бот предлагает и оформляет бесплатный пробный день (по сайту: 3 урока + 7 дней доступа), объясняет формат и что увидит ребёнок. |
| 3 | Заявка / консультация | При устойчивом интересе или прямом запросе бот фиксирует заявку и предлагает консультацию приёмной комиссии; при балле 80+ — немедленная эскалация оператору (карточка в Telegram). |
| 4 | Подбор тарифа | Менеджер/бот подбирает тариф из 1.3 под формат и цели семьи; для «индивидуального репетитора» (не фикс. цена) — ориентир через движок КП (`kp.py`) до подключения человека. |
| 5 | Документы | Бот присылает чек-лист документов для дистанционной подачи, статус отслеживается в CRM (`/orders`). |
| 6 | Договор | Оформление ведёт менеджер приёмной комиссии (в т.ч. рассрочка через Тинькофф) — вне зоны бота, бот только информирует о шаге и следующем действии. |
| 7 | Оплата | Не кнопка «Оплатил ✅» из типового ТЗ — здесь подписка + рассрочка + договор, поэтому бот доводит лида до кнопки **«Передать менеджеру ✅»** и передаёт человеку (решение зафиксировано 2026-08-12, см. `og1/PLAN.md` п.4). |
| 8 | Зачислен | Бот поздравляет, сообщает следующий шаг (доступ к личному кабинету/платформе, расписание), CRM переводит статус в «Зачислен». |

### 1.3. Прайс / тарифы

**Закрыто.** Полная сетка по классам взята напрямую с og1.ru/czena (страница-калькулятор
стоимости, значения зашиты в HTML-разметке страницы, не додуманы) — сверено 2026-08-12,
согласуется с отдельными тарифными страницами og1.ru/tarif-shkolniy и og1.ru/tarif-uchenicheskij.

**Школьный** (очно, живые уроки), ₽/мес:

| Класс | Цена |
|---|---|
| 3 | 28 310 |
| 4 | 31 250 |
| 5 | 31 800 |
| 6 | 31 800 |
| 7 | 31 800 |
| 8 | 31 800 |
| 9 | 34 650 |
| 10 | 34 650 |
| 11 | 38 850 |

**Ученический** (очно-заочно, потоковые лекции), ₽/мес — **тариф начинается с 5 класса**,
для 3–4 класса на сайте не предлагается:

| Класс | Цена |
|---|---|
| 5 | 12 050 |
| 6 | 12 050 |
| 7 | 12 050 |
| 8 | 12 050 |
| 9 | 16 050 |
| 10 | 17 750 |
| 11 | 23 100 |

**Прочие тарифы:**

| Тариф | Формат | Цена | Условия/примечания |
|---|---|---|---|
| Любознательный | самоподготовка | 124 ₽/мес | Акционная цена; базовая (зачёркнутая на сайте) — 500 ₽/мес |
| Аттестация | прикрепление для аттестации | 15 000 ₽ (разово) | Прикрепление с 5 по 11 класс; за 9 и 11 класс сама аттестация не проводится (ГИА отдельно) |
| Индивидуальные репетиторы | подготовка к ОГЭ/ЕГЭ | не фикс. цена — расчёт по параметрам | См. 1.4, движок КП (`kp.py`) |

**Скидки (og1.ru/czena):** 10% отличникам, 10% за второго и последующих детей из семьи, до 20%
при бронировании места на 2025–2026 год (действует до 15.06, для новых договоров). Рассрочка
Тинькофф — от 23 438 ₽/мес на «Школьный».

**Решено 2026-08-12:** бот скидки не упоминает и не предлагает — это на усмотрение менеджера
приёмной комиссии при оформлении договора (шаг 6 воронки, 1.2). В код ничего добавлять не нужно:
шаблоны бота (`ORIENTIR_TEMPLATE`, `HANDOFF_MESSAGE_TEMPLATE` и т.д. в `prompts.py`) и так не
содержат упоминаний скидок.

`catalog.py` обновлён: `TARIFFS` теперь хранит полную сетку по классам (`GRADE_PRICING`), не
только границы диапазона.

### 1.4. Справочник ставок для репетиторов («под запрос»)

**Уточнено заказчиком:** чёткой сетки по предметам/уровням нет, единая ставка — **от 950 ₽/час**.
Обновлено в `catalog.py` (`TUTOR_RATE_PLACEHOLDER = 950.0`), движок КП (`kp.py`) считает по этой
ставке на все предметы и уровни без деления, плюс `OVERHEAD_PCT = 15%`, `MARKUP_PCT = 25%` —
проценты по-прежнему черновые дефолты, отдельно заказчиком не подтверждались.

| Предмет | Уровень | Ставка / час | Мин. часов в неделю |
|---|---|---|---|
| любой | любой | от 950 ₽ | не указано |

Формула даёт «от» — то есть 950 ₽/час это нижняя граница себестоимости работы репетитора до
накладных/наценки; итоговая цена клиенту в `ORIENTIR_TEMPLATE`/`kp.compute()` уже выше этой
ставки за счёт накладных и наценки. Если по факту ставка варьируется по предмету/уровню сильнее,
чем «от X» — можно донастроить `TUTOR_RATE_PLACEHOLDER` позже, но для MVP этого достаточно.

### 1.5. Слова-сигналы для родительских групп

*(без изменений — уже подробно заполнено заказчиком в оригинале и перенесено в
`config.py:GROUP_SIGNAL_WORDS`, короткий список из ~11 фраз; полный список из брифа — 20
категорий, там кандидатов на слова-сигналы в разы больше)*

**Закрыто 2026-08-12.** `GROUP_SIGNAL_WORDS` в `config.py` расширен до всех 20 категорий из
брифа (~140 фраз, один плоский список). Решено (заказчик): единый тон реакции на все сигналы —
без отдельной более мягкой логики для «буллинг/травля» на этапе MVP.

---

## 2. Решение по риску: персональные данные детей

**Закрыто — ответы даны и подтверждены заказчиком напрямую 2026-08-12** (не юристом формально,
но пользователь лично подтвердил и берёт на себя это решение):

| Вопрос | Ответ |
|---|---|
| Можно ли хранить в переписке возраст/имя/школу ребёнка, причину ухода? | Да, можно |
| Нужно ли обезличивание данных при хранении в БД бота? | Нет, не нужно |
| Требуется ли согласие на обработку ПДн до начала диалога с ботом? | Нет, не нужно |
| Соответствие 152-ФЗ — кто подтверждает, к какому сроку? | Подтверждает заказчик лично, сроком — сейчас (принято 2026-08-12) |

**Что это значит для кода:** технической блокировки больше нет — можно разрешить
`dialog_context`/полям лида содержать реальные детали о ребёнке без обязательного обезличивания
и без экрана согласия перед первым сообщением бота. `db.py`/схема как есть (свободнотекстовые
детали живут в `dialog_context`) уже это допускает; отдельно ничего доделывать не нужно.

⚠️ Это управленческое решение заказчика, а не юридическое заключение — если в будущем понадобится
формальное подтверждение соответствия 152-ФЗ (например, для проверяющих органов), потребуется
отдельная юридическая консультация. Для целей разработки и запуска MVP вопрос закрыт.

---

## 3. Технические секреты и доступы

В самом брифе поля отмечены «да» (готовы), но значения передаются не через документ — верно,
в этом файле их тоже нет. Актуальный список переменных окружения и что именно они дают —
`.env.example` в корне репозитория и раздел «Работа с секретами» в `CLAUDE.md`. На момент
последнего смоук-теста (2026-08-12) `main.py` стартует и без них (с warning вместо падения), но
для боевого прогона нужны реальные `TG_API_ID/HASH`, аккаунт userbot'а, `BOT_TOKEN`,
`ANTHROPIC_API_KEY`, chat ID оператора приёмной комиссии.

---

## 4. Организационные решения

| Вопрос | Ответ |
|---|---|
| Объём первой версии | Решено и уже реализовано 2026-08-12: скоринг → прогрев → эскалация → CRM + движок КП для «индивидуальных репетиторов» — в MVP. «Места в классе» / Priority Score (аналог производственной очереди, шаг 10 исходного ТЗ) **сознательно не реализованы** — в `og1/PLAN.md` это была лишь аналогия, не подтверждённая потребность. |
| Подтверждение начинать спеку и разработку | Да, уже дано и уже сделано — полный MVP-код лежит в корне репозитория (`config.py`, `db.py`, `catalog.py`, `prompts.py`, `guard.py`, `kp.py`, `pipeline.py`, `bot.py`, `reminders.py`, `crm.py`, `main.py` + `catcher_*.py` для автосбора лидов), 28/28 тестов зелёные. Открытые пункты выше (1.1 текст, 1.3 полная сетка цен, 1.4 ставки репетиторов, раздел 2 юридически) — это то, что осталось донастроить поверх уже работающего каркаса, а не блокер для старта разработки. |

---

## Итог: что реально блокирует боевой запуск

1. ~~Юридический ответ по разделу 2 (ПДн детей, 152-ФЗ)~~ — закрыто: заказчик подтвердил
   лично 2026-08-12.
2. ~~Точная сетка цен по классам для «Школьный»/«Ученический» (1.3)~~ — закрыто: взято с
   og1.ru/czena.
3. ~~Справочник ставок репетиторов (1.4)~~ — закрыто: ставка от 950 ₽/час подтверждена заказчиком.
4. ~~Финальный текст Шаблона А (1.1)~~ — закрыто: `TEMPLATE_A_OG1` переписан дословно под бриф.
5. ~~Слова-сигналы (1.5)~~ — закрыто: расширены до полных 20 категорий брифа, один тон на все.
6. ~~Скидки~~ — закрыто: бот не упоминает, только менеджер при оформлении договора.
7. **Реальные секреты** (раздел 3) — единственное, что осталось: `TG_API_ID/HASH`, `BOT_TOKEN`,
   `ANTHROPIC_API_KEY`, `OPERATOR_CHAT`. Заказчик подтвердил готовность передать 2026-08-12.

Содержательных пробелов в брифе больше нет. Остаётся только техническая часть — передать секреты
в `.env` (не в этот документ и не через git) и один раз прогнать боевой смоук-тест.

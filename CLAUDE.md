# CLAUDE.md

## Что это за проект

Материалы курса **«Альфа Курс»** (автор — Адель Кадырова) по автоматизации продаж с ИИ в Telegram,
плюс техническое задание на боевую реализацию главного инструмента курса — **Telegram-бота
продаж с движком расчёта КП и производственной очередью**. Все исходные материалы (уроки,
промпты, методика, ТЗ) лежат в [`materials/`](materials/). Кода пока нет — **следующий шаг —
имплементация бота по ТЗ**.

**Актуальное ТЗ:** `materials/ТЗ на бота продаж с расчётом КП и веб-CRM.md`. Оно включает в себя
весь текст старого `materials/ТЗ на Telegram-бота прогрева и продаж.md` (шаги 2–8 ниже, дословно,
просто перенумеровано) и добавляет движок расчёта КП + производственную очередь (шаги 9–10 ниже).
Старый файл оставлен для истории, но как источник истины для реализации **устарел** — ориентироваться
на новый.

## Как это работает (конвейер лида от поиска до оплаты)

Важно: «Ловец лидов» (шаг 1) — **ручной инструмент, не часть бота**. Всё остальное
(шаги 2–10) — один автоматизированный процесс (userbot + бот-кассир + веб-CRM + движок КП),
описанный в актуальном ТЗ.

### Шаг 0 — Первый бот (разминка, урок 00)
Обучающий шаг курса, не связан технически с боевой системой: простой Telegram-бот на
no-code кубиках, подаёт факт (курс валюты) «голосом бизнеса».
→ `materials/Дополнительные материалы к уроку.md`, `materials/Презентация к уроку.md`

### Шаг 1 — Ловец лидов: находим, кому написать первым (урок 01, ручной)
Не бот, разовый прогон промпта:
1. Выгружаешь сообщения из открытой группы (Telegram/VK/B2B).
2. Заполняешь ICP (боль, слова-маркеры, стоп-слова).
3. Промпт П1 «Ловец тёплых лидов» находит сообщения людей, которые *ищут* решение (по смыслу, не по ключевым словам).
4. На выходе — список: цитата + повод + ссылка + готовый заход.
5. Ты сам вручную пишешь этим людям первым — бот тут не участвует.

После того как человек ответил, он становится «откликнувшимся лидом» и попадает в шаг 2.
→ `materials/Дополнительные материалы к уроку 3.md` (раздатка урока 01; **несовпадение
номера в имени файла — контент верный, просто так сохранилось**), `materials/Презентация к уроку (1).md`

### Шаг 2 — Бот обрабатывает входящее (ядро, ТЗ раздел 4.1)
Userbot-продавец (MTProto, слушает личку и группы) на каждое входящее:
1. **Проверка безопасности** — фильтр prompt-инъекций, до всего остального.
2. **Вопрос о статусе своего заказа?** → отвечает по факту заказа, дальше не идёт (см. шаг 7).
3. **Скоринг П1** — LLM оценивает сообщение 0–100 по методике `materials/Гайд по скорингу лидов.md`
   (источник истины: ступени L1–L5 + бонусы/штрафы → балл).
4. **Действие по баллу (бэнду):**
   - 0–19 холодный → молчим;
   - 20–79 тёплый/горячий → касание П5 (сообщение голосом бизнеса под этап воронки);
   - 80–100 кипящий → эскалация (шаг 4).

### Шаг 3 — Прогрев затихших (урок 02, ТЗ 4.2)
Раз в сутки фоновая задача проверяет, кто замолчал, и шлёт следующий шаг догрева по порогу тишины:
- ≥1 день → «польза» без CTA;
- ≥3 дня → «доверие» (кейс + вопрос-крючок);
- ≥7 дней → «мягкий дедлайн», дальше цикл останавливается.
Новое сообщение от лида сбрасывает цикл на начало.
→ `materials/Раздаточные материалы.md`, `materials/Презентация к уроку (2).md`

### Шаг 4 — Эскалация и передача в «первую линию» (урок 03, ТЗ 4.3)
При балле 80+:
1. Из диалога определяется позиция заказа (по прайсу или «под заказ»).
2. Создаётся заявка в CRM, определяется отдел (сервис/склад/производство).
3. **Если отдел = производство** → синхронно считается КП (шаг 9) и цена заявки берётся оттуда;
   не собрать смету → `needs_estimator`, цену вручную ставит технолог в CRM.
4. Формируется ссылка на оплату.
5. Лиду шлётся deep-link на **бота-кассира** (инлайн-кнопки умеет только бот, не userbot — ограничение Telegram).
6. Оператору дублируется карточка эскалации.
→ `materials/Раздаточные материалы (1).md`, `materials/Презентация к уроку (3).md`

### Шаг 5 — Оплата (бот-кассир, ТЗ 4.4)
1. Лид переходит по ссылке → карточка заказа с кнопкой «Оплатил ✅».
2. Нажимает → заявка `paid`, воронка двигается.
3. Бот сразу шлёт «что дальше» (запись на сервис / комплектация / срок производства — зависит от отдела).

### Шаг 6 — Напоминания о неоплаченных (ТЗ 4.5)
Лесенка: +30 мин → +1 день → +3 дня → автозакрытие через 3 дня 30 мин.
Если бот-кассир выключен, напоминания шлёт сам userbot живым текстом.

### Шаг 7 — CRM и статус заказа (ТЗ 4.6–4.7)
Мини веб-CRM в том же процессе: менеджер двигает статус
(`Оплачено → В работе → Готово → Выдано`), при каждой смене лиду уходит уведомление.
Если лид сам спрашивает про статус (см. шаг 2.2) — ответ по факту заказа, без скоринга и продажи.

### Шаг 8 — Защита от инъекций (ТЗ 4.8/8.2, действует на каждом шаге 2–7)
До любого LLM-вызова — эвристика + LLM-классификатор на попытки взлома/выманивания данных.
Срабатывание → карточка оператору, чат лида молча блокируется (разблокировка вручную).

### Шаг 9 — Движок расчёта КП (урок 04, ТЗ раздел 5)
Детерминированный расчёт, LLM только матчит состав, код считает сумму:
1. Матч изделия на техкарту (BOM + нормы работ) или, если нет матча, LLM собирает состав из
   справочника материалов/работ (каждая строка валидируется — выдуманные коды отбрасываются).
2. Формула в коде: `Материалы + Работы → Прямые → + накладные% → Себестоимость → + наценка% → КП`.
3. Тот же движок даёт **ориентировочную сумму** тёплому лиду, который спрашивает цену кастома
   (read-only, без создания заявки, ТЗ 4.5) — и **боевую цену** при эскалации (шаг 4.3).
→ `materials/Раздаточные материалы урок 04.md`, `materials/Презентация к уроку 04.md`

### Шаг 10 — Производственная очередь и приоритизация (ТЗ раздел 6)
Оплаченная заявка с отделом «производство» становится производственной задачей:
1. **Priority Score** = (маржа ÷ часы работы) × коэффициент срочности по дате сдачи.
2. **Обратное планирование**: плановый старт = дата сдачи минус буфер этапа.
3. Трек: `Очередь → Заготовка → Сборка → Контроль → Готово`; движение этапа синхронизирует
   `orders.status` и уведомляет лида. Раздел CRM `/production` — очередь, сортированная по Score.
→ методика минимального запаса и алертов — `materials/Алгоритм расчёта минимального складского
запаса.md`, `materials/Раздаточные материалы урок 05.md`, `materials/Презентация к уроку 05.md`

## Материалы

Всё содержимое курса и техническая спецификация — в [`materials/`](materials/):

| Файл | Что внутри |
|---|---|
| `Презентация к уроку.md` / `Дополнительные материалы к уроку.md` | Урок 00 — первый бот |
| `Презентация к уроку (1).md` / `Дополнительные материалы к уроку 3.md` | Урок 01 — Ловец лидов (раздатка лежит в файле с «3» в имени, см. шаг 1) |
| `Презентация к уроку (2).md` / `Раздаточные материалы.md` | Урок 02 — Прогрев откликнувшихся |
| `Презентация к уроку (3).md` / `Раздаточные материалы (1).md` | Урок 03 — Первая линия 24/7 |
| `Презентация к уроку 04.md` / `Раздаточные материалы урок 04.md` | Урок 04 — Производство: КП за 3 минуты + диагностика прибыли в час (шаг 9) |
| `Презентация к уроку 05.md` / `Раздаточные материалы урок 05.md` | Урок 05 — Склад/Загрузка: «сторож склада» — наблюдение → прогноз дефицита → алерт (шаг 10) |
| `Гайд по скорингу лидов.md` | Методика скоринга v1.1 — **источник истины**, ТЗ должно ему соответствовать |
| `Алгоритм расчёта минимального складского запаса.md` | Формулы Zmin/ROP (страховой запас, точка заказа) — методика к уроку 05 |
| `ТЗ на бота продаж с расчётом КП и веб-CRM.md` | **Актуальное** полное ТЗ на боевую систему (все шаги выше, 1–10) |
| `ТЗ на Telegram-бота прогрева и продаж.md` | Устаревшая версия ТЗ (только шаги 2–8, без КП/производства) — оставлена для истории |
| `ТЗ на калькулятор коммерческих предложений.md`, `Готовый калькулятор КП (HTML-файл).html`, `Демо-данные_ прайс-лист.xlsx`, `Демо-данные_ технологические карты.xlsx` | Отдельный **standalone**-инструмент урока 04 (не часть бота): веб-калькулятор КП на столешницы с демо-данными |
| `ТЗ на калькулятор загрузки салона.md`, `Готовый калькулятор загрузки (HTML-файл).html` | Отдельный **standalone**-инструмент (другой домен-пример — салон красоты): калькулятор оптимизации загрузки/скидок за перенос записи |
| `Архитектура ИИ-диспетчера замерщиков.md` | **Не связано с ботом-продавцом** — бонусный инструмент: LLM-оркестратор + code execution для маршрутизации замерщиков (VRPTW-эвристика без внешних солверов) |
| `Дополнительные материалы к уроку 1.md`, `Дополнительные материалы к уроку 2.md` | **Не связаны с курсом** — отдельный бонусный инструмент: ежедневная проверка контрагентов по ИНН (Apify + headless Claude Code по cron) |

## Планируемая архитектура реализации (по ТЗ)

Один asyncio-процесс, общая SQLite-база:

```
main.py       — запуск (userbot + bot + CRM + фон), обработчики шагов 2 и 3, эскалация, мост к КП
pipeline.py   — скоринг П1, генерация касания П5, извлечение заказа, guard, ответ о статусе, ориентир
prompts.py    — единый источник правды по промптам/шаблонам
catalog.py    — прайс готовых позиций, поиск, маршрутизация в отдел
db.py         — SQLite: схема, миграции, лиды, заявки, очередь, сброс
bot.py        — бот-кассир: deep-link, карточка оплаты, callback, «что дальше»
reminders.py  — лесенка напоминаний об оплате (шаг 6)
guard.py      — защита от prompt-инъекций (шаг 8)
crm.py        — веб-CRM: заявки + раздел «Производство» (шаги 7, 10)
config.py     — конфиг из env, безопасные дефолты
norms.py, norms_data.py — нормативы (материалы/работы/техкарты) + текстовый фолбэк без бинарников
kp.py         — движок расчёта КП: формула, матч техкарты, LLM-сборка состава, валидация (шаг 9)
priority.py   — Priority Score + обратное планирование (шаг 10)
production_db.py — таблица production_tasks, миграции, CRUD, пересчёт приоритетов
poller.py     — обработка новых заявок производства (расчёт КП → задача)
login.py, export_session.py — разовый логин и экспорт StringSession для деплоя
tests/        — юнит-тесты чистой логики (без Telegram/LLM)
```

Таблицы: `leads`, `orders`, `production_tasks`, `handoff`, `meta` — точная схема в разделах 10
(модель данных) и 6 (производственная очередь) актуального ТЗ.

## Работа с секретами

Секреты (`TG_API_ID`, `TG_API_HASH`, `TG_STRING_SESSION`, `BOT_TOKEN`, `LLM_API_KEY` и т.д.)
только через переменные окружения, никогда в коде, промптах или коммитах — см. раздел 12
актуального ТЗ за полным списком. `TG_STRING_SESSION` даёт полный доступ к аккаунту — обращаться
как с паролем.

## Известные эксплуатационные ограничения (раздел 16 актуального ТЗ)

- SQLite нельзя держать в облачно-синхронизируемой папке (Dropbox/Яндекс.Диск).
- Одна и та же Telegram-сессия userbot'а не может использоваться из двух мест одновременно
  (иначе `AuthKeyDuplicatedError`).
- Инлайн-кнопки поддерживает только бот (Bot API), не userbot — поэтому оплата идёт через
  отдельного бота-кассира, куда лида уводят deep-link'ом.
- Бот не может написать лиду первым, пока тот не нажал Start.

## Что осталось прояснить перед стартом реализации

- Нет пока фактического кода (`main.py` и др. модули из ТЗ не созданы) — писать с нуля.
- Реальные бизнес-данные (прайс, шаблон А, воронка 1–16, слова-сигналы, реквизиты платёжного
  провайдера, нормативы материалов/работ/техкарт для КП) в репозитории отсутствуют — в ТЗ они
  плейсхолдеры и должны быть предоставлены до боевого запуска. Демо-данные (прайс-лист,
  технологические карты) есть в `materials/` — они для standalone-калькулятора КП, не для бота.
- Не решено, входят ли standalone-калькуляторы (КП на столешницы, загрузка салона) и бонусный
  ИИ-диспетчер замерщиков в объём боевой реализации бота, или это независимые демо-инструменты
  курса — уточнить у пользователя перед тем, как включать их в scope имплементации.

## Agent skills

### Issue tracker

Локальный markdown-трекер: спека и тикеты живут как файлы в `.scratch/<feature-slug>/`, без GitHub/GitLab. См. `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: один `CONTEXT.md` + `docs/adr/` в корне репозитория (появятся по мере фиксации терминологии и решений). См. `docs/agents/domain.md`.
